name: exptools
channels:
- defaults
dependencies:
- pytz
- termcolor
- pip:
  - aiofiles
  - base58
  - butter
  - tzlocal
  - websockets
  - yaql
  - tox # requires conda's virtualenv
- wheel
- pylint
- pytest
- pytest-cov
- virtualenv
