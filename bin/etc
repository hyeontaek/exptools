#!/usr/bin/env python

import asyncio
import sys

from exptools.run_client import run_client

loop = asyncio.get_event_loop()
argv = sys.argv[1:]

sys.exit(loop.run_until_complete(run_client(argv, loop=loop)))
